# CAPS

This repository is connected to `zemcan/CAPS`.

Initial project scaffold created from the local `smart` workspace.

## Structure

- `src/`: application code
- `docs/`: project notes

## Quick start

Run the sample entry point:

```bash
python src/main.py
```
